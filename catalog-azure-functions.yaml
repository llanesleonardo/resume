apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: azure-functions-api
  description: Azure Functions backend used by the resume app
spec:
  type: http
  lifecycle: production
  owner: llanesleonardo
  # No `definition` field yet since you don’t have OpenAPI spec
